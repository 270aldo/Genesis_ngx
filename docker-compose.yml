# =============================================================================
# Genesis NGX - Docker Compose (Development)
# =============================================================================
# NOTE: For ADK-based development, use `adk web` instead of docker-compose.
# This file is kept for reference and potential future use.
# =============================================================================

services:
  # Supabase local (optional - you can also use Supabase Cloud)
  # supabase:
  #   image: supabase/postgres:15.1.0.147
  #   ports:
  #     - "54322:5432"
  #   environment:
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: postgres
  #   volumes:
  #     - supabase_data:/var/lib/postgresql/data

  # ADK Development Note:
  # =====================
  # Agents are now defined using Google ADK and run with:
  #   adk web          # Local playground
  #   adk run agent    # Run specific agent
  #   adk deploy       # Deploy to Agent Engine
  #
  # See adk.yaml for agent configuration.

networks:
  default:
    name: genesis-network

# volumes:
#   supabase_data:
